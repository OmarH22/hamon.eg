/** Shared class strings, so a button looks the same wherever it appears. */

export const primaryButton =
  'press inline-flex items-center justify-center rounded-full bg-ink px-8 text-[15px] ' +
  'font-medium text-ivory shadow-lift transition-colors hover:bg-black ' +
  'focus-visible:outline-offset-4 disabled:cursor-not-allowed';

export const pillButton =
  'press inline-flex items-center justify-center rounded-full bg-ink px-5 text-[13px] ' +
  'font-medium text-ivory transition-colors hover:bg-black md:px-6 md:text-sm';

export const ghostButton =
  'press inline-flex items-center justify-center rounded-full border border-ink/25 bg-transparent ' +
  'px-7 text-[15px] font-medium text-ink transition-colors hover:border-ink/60 hover:bg-ink/[0.03]';

export const sectionLabel = 'text-[11px] font-medium tracking-[0.18em] text-subtle';
